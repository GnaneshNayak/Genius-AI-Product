import React from 'react';

type Props = {
  title: string;
  description: string;
  iconColor: string;
  bgColor: string;
  icon: any;
};

const Heading = (props: Props) => {
  return <div>Heading</div>;
};

export default Heading;
